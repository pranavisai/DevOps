1. YMAL is used to represent data.
2. A list is ordered, and a dictionary is unordered.
3. to comment use -> #

## YMAL vs JSON
1. In YMAL, we use a dash (-) to denote an iteam in a list, in JSON we use square brackets to define a list with each item in the list seperated by a comma.
2. Converting YAML to JSON and vice versa -> json2ymal.com
3. JSON -> JavaScript Object Notation.

## JSON Path Query
1. The root of the JSON without a name is denoted by $.
2. JSONPath Query: JSONPath is a query language used to extract data from JSON documents
3. Any result we get when we run a query is encapsulated within a pair of square brackets.
4. ? -> used to specify a criterion.
5. @ -> each item in the list.
6. == -> equal to
7. != -> not equal to
8. @ in [1, 2, 3] -> example
9. @ nin [1, 2, 3] -> example not in
10. "*" -> It is used as a wildcard. To iterate through all the elements.
